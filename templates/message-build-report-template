Template @TEMPLATE_NAME@ @VERSION@ for Qubes @RELEASE_NAME@, see comments below for details and build status.

If you're release manager, you can issue GPG-inline signed command (depending on template):

* `Upload-template @RELEASE_NAME@ @TEMPLATE_NAME@ @VERSION@ templates-itl` (available @MIN_AGE_DAYS@ days from now)
* `Upload-template @RELEASE_NAME@ @TEMPLATE_NAME@ @VERSION@ templates-community` (available @MIN_AGE_DAYS@ days from now)

Above commands will work only if package in testing repository isn't superseded by newer version.

For more information on how to test this update, please take a look at https://www.qubes-os.org/doc/testing/#updates.
