Package @PACKAGE_NAME@ has been pushed to the @RELEASE_NAME@ stable repository for Debian @DIST@ template. If you want to install the update simply follow the standard update procedure, for example:
```
sudo apt-get update && sudo apt-get dist-upgrade
```
